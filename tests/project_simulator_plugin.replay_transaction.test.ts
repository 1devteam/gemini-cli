import { describe, expect, it } from 'vitest';
import createProjectSimulatorPlugin from '../project_simulator_plugin.js';

const ctx = { cwd: process.cwd(), fs: { exists: async () => false, readFile: async () => '{}' } } as never;

describe('ProjectSimulatorPlugin replay-transaction scenarios', () => {
  it('classifies replay-transaction scenario through simulator output', async () => {
    const plugin = createProjectSimulatorPlugin();
    const simulate = plugin.getCommands().find((c) => c.name === 'simulate-scenario');

    if (!simulate) throw new Error('simulate-scenario command not found');

    const result = await simulate.handler(
      { scenario: 'replay transaction duplicate request nonce reuse idempotency failure repeated transaction path' },
      ctx,
    );

    expect(result.data.scenarioKind).toBe('replay-transaction');
  });
});
